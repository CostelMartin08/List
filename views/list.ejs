<%- include('header'); -%>

<div class="box" id="heading">
     <h1> <%= ora %></h1>
   </div>

  <div class="box">
<% listTitle.forEach(function(item){ %>
  <form action="/delete" method="post">
   <div class="item">
  <input name ="checkbox" type="checkbox" value="<%= item._id%>" onChange = "this.form.submit()" >
   <p><%= item.name  %></p>
   </div>
  <input type="hidden" name="listName" value="<%= ora %>" >  
   </form>
<% }) %>

<form class="item" action="/" method="post">
  <input type="text" name="newItem" value="" placeholder="Obiect nou">
  <button type="submit" name="list" value= <%= ora %> >+</button>
<%- include('footer'); -%>
